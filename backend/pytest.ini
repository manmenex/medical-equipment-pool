[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    postgres: requires a real PostgreSQL database (set POSTGRES_TEST_DATABASE_URL); skips automatically if unreachable. Run only these with: pytest -m postgres
