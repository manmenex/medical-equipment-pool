fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
sqlalchemy>=2.0.32
asyncpg>=0.29.0
alembic>=1.13.2
redis>=5.0.8
PyJWT>=2.9.0
bcrypt>=4.1.0
openpyxl>=3.1.5
APScheduler>=3.10.4
python-multipart>=0.0.9
email-validator>=2.2.0

# Roadmap PR18D (docs/design/PR18_PRINTING_EXPORT_PLAN.md §10): backend
# HTML/CSS-to-PDF renderer for report PDF export. Requires the native
# Pango/HarfBuzz packages installed in backend/Dockerfile. Exact-pinned
# (not the repository's usual floor-pin) because the approved renderer
# comparison and the font-corruption finding (docs/DECISION_LOG.md) are
# both specific to this exact version's font-subsetting behavior -- an
# untested newer release must not be picked up silently.
weasyprint==69.0

# testing / local sqlite fallback for fast unit tests
aiosqlite>=0.20.0
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0

# Roadmap PR18D: PDF structure/text parser used only by the test suite to
# validate generated PDF content (embedded fonts, extracted text) -- never
# imported by application/runtime code. Exact-pinned alongside weasyprint
# above for the same reason: the approved version comparison tested this
# exact release.
pdfplumber==0.11.10
