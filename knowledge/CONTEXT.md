# Context

**Purpose:** Current project state — the most volatile document in the knowledge layer
**Authority:** Point-in-time status only. Does not restate rules or rationale — see `knowledge/PROJECT_MEMORY.md` for the stable snapshot this file assumes.
**Update trigger:** Every merged PR, and whenever a risk or outstanding item changes
**Maintainer:** Documentation/Governance Engineer

## Current baseline

`e250638db186f8e4dc3358bd475e9cf4eebc0bc8` — squash commit of GitHub PR #50 (Roadmap PR15, PR15A slice — Observability), on branch `claude/medical-equipment-pool-0c7fz0`. This sits on top of `a43b680a5558aa322a613b3e3eba0eeb45858edf` (documentation-only post-merge governance sync recording Roadmap PR14B's completion, GitHub PR #49 — this is PR15A's actual base), which sits on top of `82e289d40811b413659e7303a1690b66275e9759` (squash commit of GitHub PR #48, Roadmap PR14, PR14B slice — Pagination Performance), which sits on top of `4d891ac8f9f1cc1ada45347d384d06fde705a97a` (documentation-only post-merge governance sync recording Roadmap PR14A's completion, GitHub PR #47), which sits on top of `ddd17b180c06a4fd2421f4886c0568876498abb2` (squash commit of GitHub PR #46, Roadmap PR14, PR14A slice — Reliability Correctness), which sits on top of `8f7ef12e1660b35021df64fc9a529495cca77e49` (squash commit of GitHub PR #45, Roadmap PR13, Search, History, and Reporting Adjustments). **Roadmap PR8 (PR8A, PR8B, and PR8C), Roadmap PR9 (PR9A and PR9B), Roadmap PR10, Roadmap PR11, Roadmap PR12, Roadmap PR13, and Roadmap PR14 (both slices — PR14A, PR14B) are all fully complete. Roadmap PR15 (Observability and Schema Hygiene) is an Epic split into slices: PR15A (Observability) is now merged; PR15B (Schema Hygiene) is the next planned item and has not started. Application metrics, tracing, dashboards, log aggregation, and alerting are not scheduled to either PR15 slice and remain open Roadmap PR15 scope pending a future slice or an explicit governance decision to remove them — Roadmap PR15 is NOT fully complete.**

## Current PR

None — this is a documentation-only post-merge sync recording GitHub PR #50's merge (Roadmap PR15, PR15A slice), mirroring the syncs that followed Roadmap PR9 (GitHub PR #35), Roadmap PR10 (GitHub PR #37), Roadmap PR11 (GitHub PR #39), Roadmap PR13 (GitHub PR #44), Roadmap PR14A (GitHub PR #47), and Roadmap PR14B (GitHub PR #49, squash `a43b680`).

**Roadmap PR11 — Frontend Terminology and Workflow UI Pass** (GitHub PR #38, squash `7708190ebf08b7212b7a73ba831263b94434d1eb`, merged from branch `feature/pr11-frontend-terminology`, baseline `66bdd547937b7741d53b16a98fe74280dee18273`). Retired "ยืม"/"คืน" (borrow/return) as user-facing UI terminology everywhere it appeared — navigation, the dispatch (`BorrowPage.tsx`) and receipt (`ReturnPage.tsx`) forms, `EquipmentDetailPage.tsx`'s CTA buttons and transaction history, and the dashboard/reports chart labels — replaced consistently by "เบิก"/"รับคืน" (issue/receive back), the same wording the codebase had already started converging toward in isolated spots. The ward field is relabeled "หอผู้ป่วยที่รับเครื่อง (บันทึก ณ วันที่เบิก)" with a caption disclaiming real-time location tracking on the dispatch form, receipt form, and equipment-detail transaction history, satisfying the Workflow Audit §7.1 acceptance criterion. Two iterative Codex review rounds, each completed on a new exact head before PR #38 was squash merged, closed a test-coverage gap: the first added the previously-missing `BorrowPage.test.tsx` and a `DispatchReceiptWorkflow.test.tsx` end-to-end test (dispatch → receipt using only the new terminology, per PR11's own explicit test requirement); the second required that workflow test be rewritten around one shared, mutable mock store so the equipment-status transitions it asserts are actually caused by the mocked `createBorrow`/`createReturn` implementations, not hand-fed per step. No backend, API, database, migration, or RBAC change — frontend-only, exactly as scoped. See `docs/DECISION_LOG.md` ("Roadmap PR11") for full rationale. Before this PR, the most recently merged governance-synchronization PR (GitHub PR #37, squash `66bdd547937b7741d53b16a98fe74280dee18273`) recorded Roadmap PR10's completion — the same production code changes (PR8A, PR8B backend, PR8B frontend, PR8C, PR9A, PR9B, PR10) and process/documentation-only or governance PRs listed below, plus PR11 itself, are the merged history this sync records:

- **GitHub PR #21** (`0ed6598`) — post-merge governance sync: brought `docs/ROADMAP.md`, `docs/DECISION_LOG.md`, `docs/BUSINESS_RULES.md`, and this knowledge layer up to date after PR #20 merged.
- **GitHub PR #22** (`06a736c`) — Test Infrastructure Cleanup: consolidated duplicated/inconsistent test helper functions (`auth_headers`, `create_ward`, `on_demand_borrow_payload`) into `backend/tests/conftest.py` as a single shared implementation. No test behavior change — verified via full local `pytest -m "not postgres"` (273 passed) and `pytest -m postgres` (78 passed) runs before merge.
- **GitHub PR #23** (`2e403fb`) — Developer Documentation: added `docs/development/{SETUP,TESTING,MIGRATIONS,CODE_REVIEW,CONTRIBUTING}.md`, written from direct inspection of current CI/Compose/Alembic config.
- **GitHub PR #24** (`f6f7c2a`) — API & Error Catalog: added `docs/api/{ERROR_CODES,dispatch,receipt,equipment,transactions}.md`, documenting the current request/response contracts and the full HTTP-status/error-code taxonomy from direct inspection of the current backend code.
- **GitHub PR #25** (`a308515`) — post-merge governance sync after PR21-PR24: brought `docs/ROADMAP.md` and this knowledge layer up to date.
- **GitHub PR #26** (`4820dba`) — **Roadmap PR8A, Atomic Receipt Concurrency Guard.** `app.crud.transaction.close()` now performs a single PostgreSQL conditional `UPDATE`, predicated on transaction ID plus `status = 'open'`, and decides the concurrency winner solely by the statement's affected rowcount — not by the pre-existing Python status check, which remains only as a fast-path for a genuine sequential repeat. Exactly one concurrent receipt request wins; every losing request rolls back **before** any business side effect (no equipment-status change, no status-history row, no audit row) and receives the existing `TRANSACTION_ALREADY_RETURNED` response — no new error code was introduced. The winning request's ORM object is explicitly refreshed from the persisted row before the equipment transition and the response are built, so the response always reflects committed state, never a stale in-memory value. Proven with deterministic PostgreSQL concurrency tests across a matrix of 1, 2, 5, 10, and 50 requests: the 1-request case verifies normal receipt behavior with no concurrency, the 2/5/10 cases synchronize the complete burst to force genuine contention, and the 50-request case synchronizes a bounded subset to prove conditional-`UPDATE` contention without exhausting the connection pool. No API contract, schema, or frontend change.
- **GitHub PR #28** (`da4d76a`) — **Roadmap PR8B, backend slice: receipt outcome contract narrowing.** `ReturnRequest.condition` (a four-value free-form string) is replaced entirely by `receipt_outcome: ReceiptOutcome` (`"usable" | "defective"`, `extra: "forbid"`, no compatibility alias). The backend alone maps `receipt_outcome` to an `EquipmentStatus` (`RECEIPT_OUTCOME_TO_STATUS`). Response splits into two mutually-exclusive fields, `receipt_outcome` (current) and `legacy_condition_on_return` (pre-PR8B history). No database migration. See `knowledge/adr/ADR-006-receipt-outcome-contract.md` and `docs/DECISION_LOG.md` ("Roadmap PR8 (PR8B slice)").
- **GitHub PR #29** (`d3e027b`) — **Roadmap PR8B, frontend slice: adopt `receipt_outcome`.** `frontend/src/types/index.ts`, `services/borrow.ts`, and `pages/ReturnPage.tsx` now submit `receipt_outcome` (a two-choice usable/defective selector) instead of the retired `condition` field; no lifecycle-state mapping added to the frontend. Deployed together with GitHub PR #28 per the coordinated-release requirement. `docs/TECH_DEBT.md` TD-006, which tracked the frontend/backend gap, is now `Closed`.
- **GitHub PR #30** (`4af6a4c`) — post-merge documentation sync after Roadmap PR8B: closed TD-006, updated ADR-006/DECISION_LOG/ROADMAP to reflect PR8B's completion.
- **GitHub PR #31** (`f923f0a`) — **Roadmap PR8C: race-loss-vs-genuine-repeat receipt rejection.** A losing receipt request now receives one of two distinguishable, machine-readable codes — `TRANSACTION_ALREADY_RETURNED` (genuine sequential repeat) or `RECEIPT_RACE_LOST` (this request's own read observed the transaction OPEN, but it lost the conditional-close race) — both still `409 Conflict`. `RECEIPT_RACE_LOST`'s wording attributes the outcome to another *request*, not another person. The frontend (`ReturnPage.tsx`) branches on the response's `code` field, never on free-text `detail`. No lifecycle, schema, migration, or request-contract change.

- **GitHub PR #32** (`94a14b8`) — post-merge documentation-only follow-up recording Roadmap PR8's completion (PR8A/PR8B/PR8C) across `docs/ROADMAP.md` and the knowledge layer. No production code change.
- **GitHub PR #33** (`9cef841`) — **Roadmap PR9, PR9A slice: audited ward correction (backend).** New `POST /api/v1/transactions/{transaction_id}/correct-ward` endpoint (`WardCorrectionRequest {ward_id: UUID, reason: str(1-500, trimmed), extra:"forbid"}`, response is the existing `TransactionOut`). Every correction writes an audit row; a same-ward request is rejected as a no-op (`409 WARD_CORRECTION_NOOP`, no audit written) and a concurrent stale-read loss is rejected distinctly (`409 WARD_CORRECTION_CONFLICT`). Authorization was temporarily Administrator-only (`app.api.v1.deps.WARD_CORRECTION_ROLES = (ROLE_ADMIN,)`) at merge time — deliberately conservative pending Roadmap PR10's role-mapping consolidation, since no Equipment Pool Staff equivalent role was confirmed in the workflow audit at the time. Now superseded by PR10 (see GitHub PR #36 below): Equipment Pool Staff also has ward-correction access. No schema or migration change; no lifecycle-state change; not a ward-transfer or dispatch/receipt contract change.
- **GitHub PR #34** (`bfe8a42`) — **Roadmap PR9, PR9B slice: audited ward correction (frontend).** Consumes PR9A's merged contract exactly, admin-only visibility (`frontend/src/hooks/useAuth.ts`'s `canCorrectTransactionWard`, mirroring `WARD_CORRECTION_ROLES`). Two entry points sharing one `WardCorrectionAction`/`WardCorrectionDialog` pair: `frontend/src/pages/ReturnPage.tsx` (an OPEN transaction) and `frontend/src/pages/EquipmentDetailPage.tsx`'s cursor-paginated transaction history (OPEN or CLOSED, real transaction UUIDs only — never inferred from the equipment ID or status-history rows). Ward-list load/error/retry state, full keyboard focus containment and restoration (including the pending-submission edge case), and complete machine-readable error-code mapping, all added across three review rounds before merge. No backend, schema, migration, or lifecycle change. **Roadmap PR9 (both slices) is now fully complete.**
- **GitHub PR #35** (`bc1b163`) — post-merge documentation-only governance sync recording Roadmap PR9's completion. No production code change.
- **GitHub PR #36** (`53340f6`) — **Roadmap PR10: Role Model Consolidation (3 roles).** Replaced the legacy 5-role model with the confirmed 3-role model. **Roadmap PR10 is now fully complete**, and the temporary admin-only ward-correction rule from PR9A above is superseded by the confirmed 3-role matrix.
- **GitHub PR #37** (`66bdd54`) — post-merge documentation-only governance sync recording Roadmap PR10's completion. No production code change.
- **GitHub PR #38** (`7708190`) — **Roadmap PR11: Frontend Terminology and Workflow UI Pass.** See `docs/DECISION_LOG.md` ("Roadmap PR11") for full detail. **Roadmap PR11 is now fully complete.**
- **GitHub PR #39** (`2944210`) — post-merge documentation-only governance sync recording Roadmap PR11's completion. No production code change.
- **GitHub PR #41** (`9de050c`) — documentation-only **Governance PR** (not a post-merge completion sync — it resolved an open Roadmap-identity conflict *before* GitHub PR #40 merged, unlike the post-merge syncs listed above/below): classified GitHub PR #40 as an **unnumbered Post-PR11 Frontend Dashboard UX Follow-up**, resolving finding PR40-H1 (its originating task description had used "PR12," conflicting with this repository's actual Roadmap PR12, Inventory Import). Roadmap PR12 and Roadmap PR13 were left unchanged. See `docs/DECISION_LOG.md` ("Governance — GitHub PR #40 classification"). No production code change.
- **GitHub PR #40** (`93b6f94`) — **Dashboard & Equipment Status (unnumbered Post-PR11 frontend follow-up, not Roadmap PR12).** Rewrote `DashboardPage.tsx` to show the four confirmed lifecycle-status counts (reusing `StatusBadge.tsx`'s exported `STATUS_LABELS`), removed the trend/top-equipment charts and PM/Calibration due-soon widgets from the frontend (the backend summary kept returning `pm_due_soon`/`cal_due_soon` unused until Roadmap PR13 removed them — see the PR13 entry below), added loading/empty/error+retry states, and added a permission-aware quick-actions grid (reusing `canDispatchOrReceiveEquipment`). New `ScanPage.tsx` + `/scan` route: a neutral QR/BCM equipment lookup that opens `EquipmentDetailPage`, never committing to dispatch or receipt. No backend, API, database, migration, or RBAC change — frontend-only. Rebased onto GitHub PR #41's tip; the obsolete references in its own title/description/code comments/test descriptions that incorrectly classified it as "Roadmap PR12" were corrected before merge, per the Repository Owner's explicit instruction — legitimate historical references (review IDs, PR40-H1 finding history) were preserved, not rewritten. See `docs/DECISION_LOG.md` ("Governance — GitHub PR #40 classification") for the numbering-conflict rationale.
- **GitHub PR #42** (`0974735`) — post-merge documentation-only governance sync recording GitHub PR #40's merge, PR #39/#41's baseline chain, and the PR #40 merge-process exception. No production code change.
- **GitHub PR #43** (`94554a3`) — **Roadmap PR12: Inventory Import (update-only).** Administrator-only upload → preview → commit workflow, matching rows to existing equipment by canonical BCM Code. Import never creates new equipment and never generates, derives, or synthesizes `asset_number` (ADR-002) — a row with no matching BCM Code fails validation, directing the operator to the standard Equipment Master workflow first. Migration `0010_inventory_import_columns.py` adds `equipment.asset_id`/`raw_source_status` (both additive, nullable). Four independent Codex reviews on Draft PR #43 drove the final shape, including a Repository Owner architectural decision to ship update-only after two `asset_number` policies (BCM-derived, then a random placeholder) were each independently rejected as ADR-002 violations. Create-from-import is deferred follow-up scope, not a permanent prohibition. See `docs/DECISION_LOG.md` ("Roadmap PR12 — Inventory Import (update-only)") for the full four-round review chronology. **Roadmap PR12 is now fully complete.**
- **GitHub PR #44** (`878ebd9`, later rebased) — post-merge documentation-only governance sync recording GitHub PR #43's merge (Roadmap PR12) and, after a review-fix round, correcting two historical-record accuracy findings (GitHub PR #42's provenance role; PR13's then-in-progress status) and — after GitHub PR #45 (Roadmap PR13) subsequently merged — rebased onto the new baseline to record PR13's completion. No production code change.
- **GitHub PR #45** (`8f7ef12`) — **Roadmap PR13: Search, History, and Reporting Adjustments.** `dispatch_type`/`routine_round`/`from_date`/`to_date` query-parameter filters on `GET /transactions`; removed `pm_due_soon`/`cal_due_soon` from the dashboard summary (never rendered by `DashboardPage.tsx` since GitHub PR #40); dispatch-type/round distinguishability and a read-only, client-computed "days since dispatch" indicator on `EquipmentDetailPage.tsx`'s transaction history. Verified `search_bcm` (Roadmap PR5) already fully satisfied ADR-003, so no new search code was needed. Two independent Codex reviews on Draft PR #45 drove the final shape: the first found the date-range upper-bound arithmetic could raise `OverflowError` for `to_date=9999-12-31` and that a reversed range was silently accepted; fixed by computing the bound without incrementing the date (never overflows) and rejecting a reversed range with a structured `400`. The second recorded APPROVE WITH NON-BLOCKING COMMENTS. No migration; no changes to any other Roadmap PR's scope. See `docs/DECISION_LOG.md` ("Roadmap PR13 — Search, history, and reporting adjustments") for the full review chronology. **Roadmap PR13 is now fully complete.**
- **GitHub PR #46** (`ddd17b1`) — **Roadmap PR14, PR14A slice: Reliability Correctness.** PATCH nullable-field correctness (`app.crud.equipment.update()`/`app.crud.user.update()` rewritten to a two-pass validate-then-mutate: reject an explicit null on required fields and `bcm_code`/`item_no` — `NON_CLEARABLE_IDENTITY_FIELDS`, ADR-002 — with zero mutation/audit event, then apply every other field unconditionally, so nullable business fields can now genuinely be cleared, fixing a bug where every explicit-null PATCH was previously silently discarded); scheduler N+1 fix (`app.worker.scheduler.check_pm_cal_due()` now loads the notification recipient list at most once per run, and not at all when nothing is due); and a transaction boundary audit (`docs/audits/05-pr14a-transaction-boundary-audit.md`, four commit-site categories, no atomicity drift found, architecture intentionally left unchanged). One Codex review round on Draft PR #46 (substantive decision REQUEST CHANGES, shown as `COMMENTED` since the reviewing account owns the PR) found the `User.phone`-clearing test asserted HTTP 200 only (insufficient — the pre-fix implementation could also return it), the transaction-boundary audit had swapped login-success/failure line references and omitted the `last_login_at` detail, the PR description understated the API/data impact, and `IMMUTABLE_IDENTITY_FIELDS` was misleadingly named — all four fixed on a new exact head before merge. Deliberately excludes pagination/indexing (deferred to PR14B, evidence-gated) and operational logging (deferred to Roadmap PR15). No workflow redesign, no ADR changes, no schema/migration change. See `docs/DECISION_LOG.md` ("Roadmap PR14 (PR14A slice) — Reliability Correctness") for the full review chronology. **PR14A is now fully complete.**
- **GitHub PR #47** (`4d891ac`) — post-merge documentation-only governance sync recording Roadmap PR14A's completion. No production code change.
- **GitHub PR #48** (`82e289d`) — **Roadmap PR14, PR14B slice: Pagination Performance.** Evidence-gated (`docs/audits/06-pr14b-pagination-index-evidence.md`, `EXPLAIN (ANALYZE, BUFFERS)` gathered before any code was written): two composite `(created_at DESC, id DESC)` btree indexes (migration `0011_pagination_ordering_indexes.py`) matching the literal `ORDER BY` clause `app.crud.equipment.search()`/`app.crud.transaction.search()` already issue, dropping first-page query latency from 45.5-205.5ms to under 1ms at evidence scale. `CREATE INDEX CONCURRENTLY`, not plain `CREATE INDEX`, to avoid blocking writes on live-traffic tables; deliberately not declared on the SQLAlchemy models (TD-002). A documented, accepted limitation: deep cursor pagination (~75,000-100,000+ rows past page one) gets slower with the index present — out of reach at this system's confirmed real-world scale, not fixed (pagination-logic redesign, out of scope). Two Codex review rounds on Draft PR #48: **Round 1 was merge-blocking** — a bare `CREATE INDEX CONCURRENTLY IF NOT EXISTS` retry cannot distinguish a completed index from one left `INVALID` by an interrupted build, so Alembic could silently record success while the index stayed unusable; fixed by `_ensure_index_concurrently()`, which inspects `pg_indexes.indexdef`/`pg_index.indisvalid`/`indisready` and **fails closed** (a clear `RuntimeError` with an explicit recovery step) rather than auto-repairing, per the Repository Owner's explicit direction; also fixed a lock-semantics documentation error (a plain `CREATE INDEX`'s `SHARE` lock blocks writes only, not reads). **Round 2** recorded APPROVE WITH NON-BLOCKING COMMENT (a stale PR-description correction, fixed before merge), confirming nine PostgreSQL regression tests (interrupted-build fail-closed-with-recovery, mismatched-definition fail-closed, both-table planner assertions among them) and CI (617 tests) green. No API, pagination-logic, or `COUNT(*)` change. See `docs/DECISION_LOG.md` ("Roadmap PR14 (PR14B slice) — Pagination Performance") for the full review chronology. **PR14B is now fully complete — Roadmap PR14 (both slices) is fully complete.**
- **GitHub PR #49** (`a43b680`) — post-merge documentation-only governance sync recording Roadmap PR14B's completion. No production code change. This is the actual baseline PR15A was branched from (`docs/ROADMAP.md`, `docs/DECISION_LOG.md`, and the `knowledge/` docs each self-referentially still cited `82e289d`, PR14B's own squash commit, rather than this sync's own squash SHA, when this sync merged — corrected by PR15A's own post-merge governance sync).
- **GitHub PR #50** (`e250638`) — **Roadmap PR15, PR15A slice: Observability.** Structured JSON logging (`app.core.logging.JsonFormatter`); async-safe request/correlation-ID propagation via `contextvars` and a `logging.Filter` (`app.core.log_context`), reusing the existing request-context mechanism; exactly one bounded access-log event per request using the route template (not the raw URL); an independent `job_run_id` for scheduler runs; aggregate-only import-commit logging; and `safe_log()`, a fail-safe wrapper guaranteeing no logging call — including its own best-effort fallback report — can ever propagate an exception back into request, job, or import-commit handling. Baseline `a43b680` (GitHub PR #49, above). Three Codex review rounds on Draft PR #50, each on a new exact head: **Review 1** (review ID `4787144983`, head `746732dc2d758286d4340cf4628327e1206b8329`, CI run `30267254839`) REQUEST CHANGES (`PR15A-H1` — `configure_logging()`'s reliance on `logging.basicConfig()`'s default idempotency meant the JSON formatter could silently never install if the root logger already had a handler; `PR15A-H2` — the post-commit import-success log could turn an already-committed success into an HTTP 500 if it raised). **Review 2** (review ID `4788591587`, head `c32270e01073fb486066d5f95548282056f3b930`, CI run `30277548822`) REQUEST CHANGES (H1 resolved; `PR15A-H2R` — the fallback log call in the round-1 fix was itself still unguarded, same gap in the access-log fallback and scheduler completion logging). **Review 3** (review ID `4789829543`, head `eeae67542d02e1dc266a15979c2b02857020f872`, CI run `30286421490`, 5/5 jobs green) APPROVE WITH NON-BLOCKING COMMENTS (H2R resolved by `safe_log()`, applied to every post-success logging path, independently verified by 24/24 passing tests in `backend/tests/test_observability_logging.py`; `PR15A-M1` non-blocking deferred — exception-handler logs still use the raw request path). No schema or migration change. **No breaking API changes:** the implementation adds backward-compatible response headers only (`X-Request-ID`, `X-Correlation-ID`); existing clients continue to function without modification, and business semantics, response bodies, and status codes remain unchanged. See `docs/DECISION_LOG.md` ("Roadmap PR15 (PR15A slice) — Observability") for the full review chronology. **PR15A is now fully complete. Roadmap PR15 (the Epic) is NOT fully complete** — PR15B (Schema Hygiene) is next; application metrics, tracing, dashboards, log aggregation, and alerting remain open Roadmap PR15 scope, not scheduled to any slice.

Roadmap PR7 (both slices, GitHub PR #19/#20), Roadmap PR8 (all three slices, GitHub PR #26/#28/#29/#31), Roadmap PR9 (both slices, GitHub PR #33/#34), Roadmap PR10 (GitHub PR #36), Roadmap PR11 (GitHub PR #38), Roadmap PR12 (GitHub PR #43), Roadmap PR13 (GitHub PR #45), Roadmap PR14 (both slices, GitHub PR #46/#48), and Roadmap PR15's PR15A slice (GitHub PR #50) remain the most recent *fully completed Roadmap-numbered* items. Roadmap PR15 as a whole (the Epic) is not fully complete — see "Next planned PR" below. See `knowledge/adr/ADR-005-transaction-model.md`, `knowledge/adr/ADR-006-receipt-outcome-contract.md`, `docs/DOMAIN_MODEL.md`, and `docs/DECISION_LOG.md` for PR7, PR8A, PR8B, PR8C, PR9 (both slices), PR10, PR11, PR12, PR13, PR14A, PR14B, and PR15A.

## Next planned PR

**Roadmap PR15, PR15B slice — Schema Hygiene**, per `docs/audits/04-consolidated-implementation-plan.md` Part D, `docs/ROADMAP.md`, and the architecture-approved design (`docs/design/PR15_OBSERVABILITY_SCHEMA_HYGIENE_PLAN.md`, Revision 2, uncommitted). Not yet started; requires a separate architecture approval before implementation begins. **Roadmap PR15 as a whole is not complete once PR15B merges either:** application metrics, tracing, dashboards, log aggregation, and alerting are not scheduled to either PR15 slice and remain open Roadmap PR15 scope pending a future slice or an explicit governance decision to remove them.

## Outstanding work

- Roadmap PR9 (both slices), Roadmap PR10 (Role Model Consolidation), Roadmap PR11 (Frontend Terminology and Workflow UI Pass), Roadmap PR12 (Inventory Import, update-only), Roadmap PR13 (Search, History, and Reporting Adjustments), Roadmap PR14 (both slices — PR14A Reliability Correctness, PR14B Pagination Performance), and Roadmap PR15's PR15A slice (Observability) are merged and fully complete. Roadmap PR15's PR15B slice (Schema Hygiene) remains planned, not started, and application metrics/tracing/dashboards/log aggregation/alerting remain open Roadmap PR15 scope not yet scheduled to any slice — **Roadmap PR15 as a whole is NOT fully complete.** Create-from-import (deferred from Roadmap PR12) is confirmed future work not yet scheduled to a Roadmap PR — see `docs/ROADMAP.md`.
- Non-blocking test-hardening follow-ups recorded from the PR14A review (candidates for a small test-only PR): additional PATCH null-clearing regression coverage for the remaining nullable Equipment fields beyond `brand` (`model`, `pm_due_date`, `cal_due_date`, `category_id`, `department_owner_id`, `current_location_id`, `serial_number`); a regression test proving the two-pass validate-then-mutate ordering itself (one PATCH request mixing a valid clear with a rejected null, asserting nothing mutates); and a decision plus test suite for blank/whitespace-only string validation on `equipment_name`/`full_name`, deferred by PR14A. See `docs/DECISION_LOG.md` ("Roadmap PR14 (PR14A slice)", "Non-blocking follow-ups"). These were not bundled into PR14B (index/migration/test scope only, per Repository Owner approval).
- Confirmed future work not yet scheduled to a Roadmap PR: Shift Sessions, Standby Snapshots, managed-deployment target selection (`docs/ROADMAP.md`).
- `docs/TECH_DEBT.md` open items: TD-001 (equipment update/status `MissingGreenlet`), TD-002 (`0001_initial.py` uses current ORM metadata), TD-003 (CI now exists and fails closed, but branch protection requiring it is not enabled — partially resolved, needs re-assessment), TD-004 (naive `datetime.utcnow()`), TD-005 (temporary default/long-lived branch structure).

## Current risks

- **Branch protection is not enabled.** CI failing does not technically block a merge yet; the Repository Owner must manually re-verify CI status before every merge (`docs/KNOWN_LIMITATIONS.md`).
- **GitHub review-submission permission limitation.** Connector submission of a formal Pull Request Review can fail with `403 Resource not accessible by integration` (not a silent downgrade). Preferred fallback is a formal `COMMENTED` review submitted through an authenticated browser session, which does satisfy review evidence; a PR Conversation comment is only a last-resort, incomplete status report and never counts as completed review evidence by itself (`docs/KNOWN_LIMITATIONS.md`).
- **Default branch is still a temporary `claude/*` name**, not `main` (TD-005); repository-maintenance cutover is a separate, not-yet-executed operation (`docs/REPOSITORY_STRATEGY.md`).

## Related documents

| Concern | Document |
|---|---|
| Stable, less volatile snapshot | `knowledge/PROJECT_MEMORY.md` |
| Full roadmap detail | `docs/ROADMAP.md` |
| Conceptual change history | `knowledge/CHANGE_HISTORY.md` |
| Per-decision rationale | `docs/DECISION_LOG.md` |
